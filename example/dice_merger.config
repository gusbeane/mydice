# List of galaxy parameters file
# Putting identical galaxies one beside the other allows DICE
# to re-use the previous computation,
# and thus reduces the global execution time
Galaxy         params_files/galaxy1.params
Galaxy         params_files/galaxy2.params

# Number of OpenMP threads
Nthreads                8
# Compute the mean inter-particle distance
# Useful to define the softening length in SPH codes
MeanPartDist            0
# Allow imaginary solution for the velocity dispersion computation
AcceptImaginary         0
# Maximum number of component for a galaxy model
MaxCompNumber           20
# Output the circular velocity in an additional file
OutputRc                0
# Define a Keplerian trajectory (only when 2 galaxies are defined)
SetKeplerian		1
# Name of the final IC file
# The file extension wil be added by DICE
Filename                dice_merger
# File format of the IC file [Gadget1,Gadget2]
ICformat		Gadget2
# Cosmological parameters for Virial quantities
# Default values are taken from Planck cosmology
# Hubble constant
H0			71.0			
# Dark energy density parameter
OmegaL			0.70
# Matter density parameter
OmegaM			0.30
# Spatial curvature density paramter
OmegaK			0.00
# Normalize the sum of mass fractions to one
NormMassFact            1
# Size of the GSL integration space
GslWorkspaceSize        1000000

# Keplerian trajectory parameters
# -------------------------------
# This keywords should be set if you want a Keplerian orbit between two galaxies.
# If there are more or less galaxies, trajectories will be set according
# to the xc,yc,zc,vel_xc,vel_yc,vel_zc keywords in the galaxy parameters files.
# xc,yc,zc,vel_xc,vel_yc,vel_zc are recomputed according to the Keplerian paramters if SetKeplerian=1
# By default, galaxies are aligned along the X-axis

# Eccentricity of the Keplerian orbits
Eccentricity    1.00
# Initial distance between the galaxies [kpc]
Rinit           100.
# Pericentral distance of the Keplerian system [kpc]
Rperi           1.
# Polar angle of the normal vector of the orbital plane
OrbitPlanePhi   45.
# Azimuthal angle pf the normal vector of the orbital plane
OrbitPlaneTheta 45.


